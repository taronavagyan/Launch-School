// 1.

/* The function logs 1 to the console because although there is a variable declaration 
that 'overrides' an existing variable, it is local scope, so the global variable is the same
when called outside of the function block. */

// 2.

/* see greetings.js */

// 3.

/* see multiply.js */

// 4.

/* It doesn't log anything because it stops at return. */

// 5.

/* It logs nothing, but returns 'Yipeee!!!!'. */

