// 1.

/*
let numbers = [1, 2, 3, 4];

numbers = []; /


while (numbers.length) {
  numbers.pop();
}


numbers.splice(0);

*/

// 2.

/*
First the arrays are converted to strings, then
concatenated.
 */
