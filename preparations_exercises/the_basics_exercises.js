// 1.

/* 'John ' + 'Doe'; */

// 2.

/*
let number = 4936;
let ones = number % 10;

number = (number - ones) / 10;
let tens = number % 10;

number = (number - tens) / 10;
let hundreds = number % 10;

number = (number - hundreds) / 10;
let thousands = number % 10;
*/

/* 3.

string
boolean
number
number
undefined
object

*/

// 4.

/* Since one of the operands is a string,  the other operands are coerced into strings. */

// 5.

/* console.log(Number('5') + 10); */

// 6.

/* console.log(`The value of 5 + 10 is ${Number('5') + 10}.`); */

// 7.

/* No, it will return undefined because the value at that index is undefined. */

// 8.

/* let names = ['asta', 'butterscotch', 'pudding', 'neptune', 'darwin']; */

// 9.

/*let pets = {
  asta : 'dog',
  butterscotch : 'cat',
  pudding : 'cat',
  neptune : 'fish',
  darwin : 'lizard',
};

*/

// 10.

/* false, because one has a capital F, and the other does not. */

// 11. 

/* 3 */

// 12.

/* true, false when comparing strings, js goes character-by-character left-to-right, and 1 < 9 . */

